# UMI-Project Data Upload

This repository contains selected `session_*` folders exported from the local IMU-camera SLAM workspace.

Excluded from this upload:

- `*.bag`

The `.bag` files were intentionally left out to keep the repository within standard GitHub file-size limits.
